# Vehicle Feature Generation (BE (DB + API), AP, FE)

## Generating a new feature BE

AI spend about 15 minutes to generate a new feature. I ran the command `/add-feature-back-end` and provided.
Then I spend about 43 minutes to verify the result and adjust it. In total it took about `1 hour` to create a new feature.

## Generating a new feature

AI spend about `9:10 minutes` to generate a new feature. I ran the command `/add-admin-feature` and provided.
Then I spend about `37 minutes` to verify the result and adjust it. In total it took `46 minutes` to create a new feature.

## Generating a new feature FE

AI spend `10:50 minutes` to generate a new feature. I ran the command `/add-feature` and provided details.
Then I spend about `1 hour` to verify the result and adjust it and to create two pages:

- vehicles-page
- vehicle-details-page

In total it took about `1 hour 11 minutes` to create a new feature for the front-end.

## Conclusion

Overall, the process of generating a new feature for the vehicle feature across the back-end, admin panel, and front-end took approximately `2 hours and 57 minutes`. This includes the time spent on generating the features using AI, verifying the results, and making necessary adjustments to ensure the functionality and design meet our standards.

Also I spend about `30 minutes` to define vehicle data structure. So in total it took about `3 hours and 27 minutes` to create a new feature for the vehicle.
