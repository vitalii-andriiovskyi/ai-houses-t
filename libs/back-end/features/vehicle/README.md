# be-vehicle

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test be-vehicle` to execute the unit tests via [Jest](https://jestjs.io).

## Generating a new feature

AI spend about 15 minutes to generate a new feature. I ran the command `/add-feature-back-end` and provided.
Then I spend about 43 minutes to verify the result and adjust it. In total it took about `1 hour` to create a new feature.
